# Workflow

## Daily Run (End of Day)
1. Pull sources (public only)
2. Extract candidate leads
3. Normalize fields (address, city, state, zip, project type)
4. Deduplicate vs. historical log
5. Output daily list (CSV + JSON + human summary)
6. Store run artifacts
